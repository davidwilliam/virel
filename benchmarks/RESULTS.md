# Benchmark results

Comparative results are pending. The harness and the nine task
definitions (SPEC 14.7) are in place, but reference trajectories against
comparison stacks have not been recorded, so no numbers are published
here yet. This section will not report provisional or estimated figures:
when a run completes, its full inputs and outputs are committed under
`runs/<date>/` and the aggregate replaces this notice.

The SPEC 14.8 targets remain release gates, not launch claims.
